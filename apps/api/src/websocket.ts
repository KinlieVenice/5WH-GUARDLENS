// Placeholder entrypoint. Real-time push (e.g. live incident updates) arrives in Stage 2;
// for now this file just exists so the build wiring and process layout are in place.
console.log("websocket entrypoint — stub for Stage 2");
